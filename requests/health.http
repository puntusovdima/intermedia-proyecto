@baseUrl = http://localhost:3000/api

### 1. Health check (Ping)
GET {{baseUrl}}/ping

### 2. Test operational error (Error handler demo)
GET {{baseUrl}}/test-error

### 3. Test non-existent route (Catch-all 404 test)
GET {{baseUrl}}/non-existent
